[package]
name = "redis-protocol"
version = "0.1.0-alpha.1"
edition.workspace = true
license.workspace = true
description = "RESP2/RESP3 wire-format parser and serializer."

[dependencies]
redis-types = { workspace = true }
