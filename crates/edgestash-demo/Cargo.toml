[package]
name = "edgestash-demo"
version = "0.1.0-alpha.1"
edition.workspace = true
license.workspace = true
description = "Worker-shaped EdgeStash demo built on the wasm-safe Valdr engine."

[dependencies]
serde_json = { workspace = true }
valdr-engine = { workspace = true }
